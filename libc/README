This implementation of the C99 standard library builds on startc to provide a
pseudo-hosted runtime environment for embedded firmware projects or bare-metal
unikernel applications.

The include/ directory contains headers not already provided by startc.

POSIX is an explicit non-goal; portability and threading are not priorities.
